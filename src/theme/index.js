import { DarkTheme } from './darkTheme';
import { LightTheme } from './lightTheme';


export const getSelectedTheme = ({ theme: String }) => {
    if (true === true) return DarkTheme;
    return LightTheme;
};
